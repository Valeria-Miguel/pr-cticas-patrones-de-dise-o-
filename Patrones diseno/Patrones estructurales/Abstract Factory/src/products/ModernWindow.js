const Window = require('./Window');

class ModernWindow extends Window {
  // igual que la puerta, describimos una ventana moderna
  describe() {
    return 'ventana moderna con cristal templado.';
  }
}

module.exports = ModernWindow;
